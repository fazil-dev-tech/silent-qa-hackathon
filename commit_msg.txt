feat: finalize enterprise hackathon build with UI/UX overhaul & robust AI fallbacks

- 🎨 **Hyper-Premium UI/UX:** Applied a deep-space Cyberpunk Glassmorphism theme globally (`globals.css`). Features include animated floating mesh backgrounds, glowing Apple Vision Pro-style borders on glass cards, and neon gradient typographies.
- 🚀 **Build & Next.js Upgrades:** Fully migrated to Turbopack standards (renamed `middleware.ts` to `proxy.ts`). Resolved all strict TypeScript namespace and typecasting errors. `npm run build` now compiles natively with Code 0.
- 🛡️ **Enterprise Extension Logic:** Completely upgraded `content.js` and `background.js` to include Regex PII-scrubbing (emails/phones/cards), instant DOM Snapshotting on critical API crashes, and `chrome.storage.local` offline resilience. 
- 🤖 **AI Graceful Failover:** Engineered robust defensive programming in the NVIDIA `mistral-nemotron` API hooks. If the API hits a silent gateway timeout, the AbortController safely intercepts the hung connection after 30s and injects a clean, markdown-sanitized fallback message straight into the `contentEditable` UI fields to ensure 100% demo uptime.
- 📧 **Automated Nodemailer:** Correctly patched backend `/api/send-email` routes to correctly parse, serialize, and format the AI-generated defect payload into HTML emails using the `mohamedfazilpasha156@gmail.com` app password.
